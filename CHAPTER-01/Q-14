def unique_paths_recursive(m, n):
    if m == 1 or n == 1:
        return 1
    
    return unique_paths_recursive(m-1, n) + unique_paths_recursive(m, n-1)

print(unique_paths_recursive(7, 3)) 
print(unique_paths_recursive(3, 2))
